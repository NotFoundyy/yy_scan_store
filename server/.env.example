DATABASE_URL=postgres://store_scan:change-me@postgres:5432/store_scan
JWT_SECRET=replace-with-at-least-32-random-characters
PORT=3000
CORS_ORIGIN=*
# 初始管理员密码（仅在首次创建 admin 账号时使用），不设置则用内置默认值
ADMIN_PASSWORD=
